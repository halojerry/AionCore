tushare>=1.4.0
pandas>=1.5.0
requests>=2.28.0
PyPDF2>=3.0.0
# PDF text/section extraction for deep mode (CPU-only, no models).
# PyMuPDF is the default engine; pymupdf4llm is an optional Markdown upgrade.
# Both are optional — extraction falls back to PyPDF2 if absent.
PyMuPDF>=1.24.0
pymupdf4llm>=0.0.17
